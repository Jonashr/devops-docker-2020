
Instructions for Building and Running the images:

- cd into the backend
docker build -t docker-backend .
docker run -p 8000:8000 docker-backend

- cd into the frontend
docker build -t docker-frontend .
docker run -p 5000:5000 docker-frontend
